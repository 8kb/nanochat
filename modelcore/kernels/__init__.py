"""
Kernel wrappers modelcore components use for attention. Import directly from the submodule
(e.g. `from modelcore.kernels.flash_attn import flash_attn`) -- not re-exported here, since
flash_attn.py's own module-level `flash_attn` object would otherwise shadow the submodule
reference on this package.
"""
